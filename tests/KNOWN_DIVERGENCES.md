# Known divergences from the MATLAB pipeline

One entry per deliberate difference between `utespac/` and UTESpac MATLAB,
with its expected magnitude. MATLAB parity was retired on 2026-08-22 (user
ruling: the MATLAB products will not be assembled; the EddyPro comparison
in the audit doc is the validation reference), so this file is a record of
where and by how much the Python pipeline departs from the original, not
an acceptance statement for a comparison that runs. The `matlabCompat`
flag that reproduced the legacy physics, the golden-file test
(`tests/test_regression.py`), the `.mat` loaders in `utespac/testkit.py`
and the duplicate/typo columns kept for positional comparison went with
it; the "Behind `matlabCompat`?" column below records what the flag used
to cover. Rows marked "magnitude: to measure" have no MATLAB reference to
measure against and stay as estimates.

| Date | Where | Divergence | Was behind `matlabCompat` (retired 2026-08-22) | Expected magnitude | Why |
|---|---|---|---|---|---|
| 2026-08-22 | `flux/tables.py` R, skew | The duplicate `R_wPF_CO2` column (MATLAB R col 14) is dropped and the `skew_Theata_v` label reads `skew_Theta_v`. | — | R has 15 columns per sonic instead of 16; one header label | Parity retired (B.8); fixture re-pinned. |
| 2026-08-22 | `sonic_rotation.py` | Global planar fit applied with (b1, b2) instead of (b0, b1); b0 removed from w. | yes | u* ~18 %, LE ~20 % on the validation-site GPF; mean wPF shifts by b0 | Audit findings 1-2; Wilczak et al. 2001 eqs. 35-39. |
| 2026-08-22 | `get_data.py` | Labeled 2-D fields concatenated by header label (union of columns, NaN where absent) instead of the whole-file NaN block on a column-count change. | yes (`matlab_compat=` kwarg) | no numeric change where column sets agree; restores whole days otherwise | Audit "get_data blanks whole days"; the validation site's mid-campaign fine-wire outage. |
| 2026-08-22 | `fluxes.py` derivedT | `derivedTheader` trimmed together with the all-NaN columns of `derivedT`. | no | header only | Header/data width mismatch broke label alignment. |
| 2026-08-22 | `calc_dissipation_rate.py` | ε from the compensated structure function averaged over lags 0.1-2 s, C2 = 2.0, instead of the single lag where D_LL crosses r^(2/3). | no | ε column not comparable (old values were not an estimate of ε) | Audit finding 5; synthetic Kolmogorov test to 1 %, validation-site φ_ε ≈ 1.15 near neutral. |
| 2026-08-22 | `calc_snsp_angle.py`, `fluxes.py` | Downslope aspect and along-slope PF axis read from SiteInfo (`downslopeAspect`, `slopeAxis`); French Meadows values (30°, "v") are the fallback with a warning when `angle != 0`. | no | none for FM sites and flat sites | Audit finding 6. |
| 2026-08-22 | `fluxes.py` theta_son_air, `sonic_temperature.py` | Sonic-temperature humidity coefficient 0.51 (Schotanus 1983 eq. 5) instead of the virtual-temperature 0.61 in the mean rescale `theta_son_air`. | yes | T_air, derivedT T_son_air: ~0.1 q T ≈ 0.3 K at q = 10 g/kg; rescaled `T_air'w'` columns ~1 % | Audit finding 3; `library/writeups/sonic_temperature_flux.md`. |
| 2026-08-22 | `fluxes.py` H T_air columns | Where high-frequency humidity exists, `T_air'w'` / `T_air'wPF'` are w'T' = w'Ts' − 0.51 T̄ w'q' (Schotanus eq. 8, Liu 2001 eq. 12) instead of the mean-humidity rescale. | yes | −0.51 T̄ c_p/L_v × LE ≈ −0.06 × LE in W/m²; measured on the validation-site GPF: H bias vs EddyPro +11.1 → −0.03 W/m², RMSE 15.7 → 0.75 | Audit finding 3. |
| 2026-08-22 | `fluxes.py` kin_sen_flux, rhov_ext, rhoc_ext | WPL terms (LHflux W/m², KH2O O₂, CO2flux WPL, sample-wise external fluctuations) driven by the corrected w'T' instead of the buoyancy flux Theta_v'wPF' (MATLAB fluxes.m:1073). | yes | measured on the validation-site GPF vs EddyPro: Fc slope 0.940 → 0.975 (bias +0.63 → +0.10 µmol m⁻² s⁻¹), LE slope 0.989 → 0.984 (−0.5 % of LE; EddyPro's 1 % spectral factor is the rest of the gap) | Audit finding 4; Webb et al. 1980 eqs. 24-25, 44. |
| 2026-08-22 | `fluxes.py` Vtheta_fw | Level-local humidity (`q_ref_fast_local`) as in `fluxes.m:566`. | no (restores parity) | removes a port deviation; magnitude: to measure on multi-level FM data | Audit finding 7. |
| 2026-08-22 | `rh_to_spec_hum.py`, `get_virtual_pot_temp.py` | One saturation-vapour-pressure formula (Stull 1988 eq. 7.5.2d) and the moist denominator q = 0.622e/(P − 0.378e). | no | q_ref ~1 % (≈0.6 % denominator, ≈0.5 % e_sat); `specificHum` output may exceed 1 %; downstream ρ, θ_v, Tair well under 0.1 % | Audit finding 7; magnitude: to measure. |
| 2026-08-22 | `fluxes.py` IRGA q_ref branch | q_ref = ρ_v / (ρ_d + ρ_v) with ρ_d from (P − e). | no | q_ref ~0.5 % on IRGA-only sites; magnitude: to measure | Audit finding 7. |
| 2026-08-22 | `find_delta_time.py` | Ejection/sweep fractions over valid samples, not the full window. | no | `delta_time_ctrb` in gappy windows only | Audit finding 7. |
| 2026-08-22 | `nandetrend.py` | Linear fit on the true sample index across interior gaps. | no | detrended series with interior NaN runs only (despiking interpolates first, so usually nil) | Audit finding 7. |
| 2026-08-22 | `calc_ssitc_flags._above_canopy_sigmaw` | Stable-side σw/u* reference for 0 ≤ z/L ≤ 0.4 is Thomas & Foken 2002 (0.21 ln(f/u*) + 3.1, via Foken 2008 Table 2.12) when `SiteInfo.latitude` is set; for z/L > 0.4 and for the stable side without a latitude it is Pahlow et al. 2001 eq. 14 (1.1 + 0.9 (z/L)^0.6); MATLAB used 2.0 (z/L)^(1/8) / 1.3 on both sides. Unstable side unchanged and now cited (Foken 2008 Table 2.11). | no | SSITC flags on stable periods: model ~1.4 for 0.032 < z/L ≤ 0.4 (was 1.3–1.78), 1.62–2.0 for 0.4 < z/L ≤ 1 (was 1.78–2.0), a 15 % step at z/L = 0.4 between the two sourced forms; magnitude on flags: to measure | `library/writeups/itc_sigmaw.md`. |
| 2026-08-22 | `fluxes.py` sigma | `np.nanstd` ddof = 0 vs MATLAB `std` N − 1 (kept). | — | < 2e-5 relative at n ≈ 36 000 | Recorded convention; `_ss_dev` uses ddof = 1. |
| 2026-08-22 | `utespac/flux/` (reference, levels, engine) | Block reductions run on single columns (`averaging.block_mean`) instead of on `[series, t]` two-column stacks through `simple_avg`; numpy sums a single column pairwise and a two-column stack sequentially, so means differ in the last bit and everything downstream by a few ulp. | no | ≤ 1e-13 absolute on every output (old-vs-new A/B on the fixture, 8 option variants, LPF and GPF); NaN patterns and headers identical | Migration step 4 (`fluxes` split); not a numeric change. |
| 2026-08-22 | `rotation.yaw_rotate` (via `sonic_rotation`) | Yaw-rotation periods are the shared row blocks `round(linspace(0, n, N+1))` (`averaging.period_bounds`) instead of `searchsorted(t, t0 + k·dt)`; the yaw angle was already taken from those blocks. | no | none on the 20 Hz / 10 Hz day files (the two splits coincide there, checked on the serial-date grid); on the 1 Hz fixture 6 of 48 period edges moved by one sample (the `searchsorted` form was one float-rounding step late), which is the re-pin of the pinned fixture's `expected/` (development tree `data/fixtures/`) on 2026-08-22 | Migration step 4 (B.7 one window iterator); the `searchsorted` form depended on float rounding of `t0 + k·dt`. |
| 2026-08-22 | `fluxes.py` CO2 ppm | Header now says "moist-air molar ratio" (value unchanged). | — | none | Audit finding 7; AmeriFlux expects dry mole fraction at export. |
| 2026-08-24 | `flux/tables.py`, `flux/engine.py` | New `scaling` output table (MATLAB had none): surface-layer scales theta*_SL = −w'theta_v'/u* [K] and q*_SL = −(E_wPF/ρ_moist)/u* [g/kg] per sonic (Stull 1988 pp. 356-357), masked like Thv_wPF and E_wPF respectively. | — | new columns only; existing outputs unchanged (fixture re-pinned for the added field) | Board task: consumers re-derived the scales downstream. |
| 2026-08-25 | `flux/levels.py` theta_son | The `theta_v_son` column keeps the Ts ≈ Tv approximation `theta_son = T_son + Γ_dry·(z − z_ref)` (as MATLAB), deliberately declining upstream b845dbd's exact form `((Ts+273.15)/(1+0.51q) + Γ·dz)(1+0.61q) − 273.15`. EddyPro's buoyancy flux uses w'Ts', i.e. the convention this fork's validation matches. | — | difference to the exact form ~0.1 q T ≈ 0.3 K at q = 10 g/kg on `theta_v_son` and the Theta_v-based products | Upstream-fixes adoption 2026-08-25, item 3 (default: keep and record). |
| 2026-08-22 | `save_data.py`, `export_hf.py`, `get_data.py` | The averaged and raw pickles are no longer written: the `utespac-run-2` run netCDF is the product, the HF netCDF is written from the `Run` (`saveRawConditionedData`), `get_data` defaults to `fmt="nc"`. The fixture runner reads the run file back; the raw pin comes from the in-memory `Run`. | — | none: the run file read back equals the pickle field by field (pins unchanged) | Labeled run model, user ruling 2026-08-22 ("the end nc product only"); `tasks/archive/migration/2026-08-22_labeled-run-model.md`. |
